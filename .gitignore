# Python
__pycache__/
*.pyc

# Regenerable ChEMBL network caches (re-fetch via the *_fetch.py / fetch_actives.py scripts)
actives.json
*_actives.json
raw_*.json
# ...but the curated M2 read-across set is AUTHORED data, not a fetched cache - commit it.
!app/data/outcome_actives.json

# uv virtual environment
.venv/

# App regenerable ChEMBL cache
app/data/panel_actives.json
